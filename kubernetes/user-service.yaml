apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
  namespace: foodapp
spec:
  replicas: 2
  selector:
    matchLabels:
      app: user-service
  template:
    metadata:
      labels:
        app: user-service
    spec:
      containers:
      - name: user-service
        image: pavanreddy8/foodapp-user-service:v1
        ports:
        - containerPort: 5000
        env:
        - name: DB_HOST
          value: "user-mysql"
        - name: DB_USER
          valueFrom:
            secretKeyRef: { name: foodapp-secret, key: mysql-user }
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef: { name: foodapp-secret, key: mysql-password }
        - name: DB_NAME
          value: "userdb"
        resources:
          requests: { cpu: "100m", memory: "128Mi" }
          limits: { cpu: "300m", memory: "256Mi" }
---
apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: foodapp
spec:
  selector:
    app: user-service
  ports:
  - port: 5000
    targetPort: 5000
